apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-volume
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
