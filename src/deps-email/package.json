{
  "name": "@homelab/deps-email",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx eslint . && bunx prettier --check .",
    "lint:fix": "bunx eslint . --fix && bunx prettier --write .",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "openai": "^6.10.0",
    "simple-git": "^3.30.0",
    "yaml": "^2.8.1",
    "zod": "^4.1.11"
  },
  "devDependencies": {}
}
