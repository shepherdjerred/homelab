project: default
source:
  repoURL: "https://1password.github.io/connect-helm-charts"
  targetRevision: 1.14.0
  directory:
    recurse: true
    jsonnet:
      tlas:
        - name: operator.create
          value: "true"
    include: argocd-vault-plugin-helm
  chart: connect
destination:
  server: "https://kubernetes.default.svc"
  namespace: 1password
syncPolicy:
  syncOptions:
    - CreateNamespace=true
